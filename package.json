{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "postbuild": "lerna link",
    "clean": "rimraf \"packages/**/tsconfig.tsbuildinfo\" \"packages/**/lib\" \"**/.acot\"",
    "format": "npm-run-all format:prettier format:script",
    "format:prettier": "prettier --write .",
    "format:script": "yarn lint:script --fix",
    "lint": "npm-run-all -p lint:*",
    "lint:prettier": "prettier --check .",
    "lint:script": "eslint . --ext \".js,.jsx,.ts,.tsx\"",
    "release": "lerna publish from-package",
    "test": "lerna run --no-bail --scope \"@acot/*\" test",
    "test:ci": "yarn test --parallel",
    "versionup": "lerna version --exact --no-private --conventional-commits",
    "versionup:cancel": "git tag --points-at @ | xargs -I{} git tag --delete {} && git reset --hard @^",
    "versionup:major": "lerna version major --exact --no-private --conventional-commits",
    "versionup:minor": "lerna version minor --exact --no-private --conventional-commits",
    "versionup:patch": "lerna version patch --exact --no-private --conventional-commits"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,md,yml,json}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@acot/tsconfig": "0.0.2",
    "@types/jest": "^26.0.5",
    "@types/markdown-table": "^2.0.0",
    "@types/mock-fs": "^4.10.0",
    "@types/node": "^14.14.6",
    "@typescript-eslint/eslint-plugin": "^4.6.0",
    "@typescript-eslint/parser": "^4.6.0",
    "eslint": "^7.12.1",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-jest": "^24.0.1",
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "lerna": "^3.20.2",
    "lint-staged": "^10.5.0",
    "mock-fs": "^4.13.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.0.5",
    "prettier-plugin-packagejson": "^2.2.7",
    "rimraf": "^3.0.2",
    "scaffdog": "^0.3.0",
    "ts-jest": "^26.4.3",
    "ts-node": "^9.0.0",
    "type-fest": "^0.18.0",
    "typescript": "^4.0.5"
  }
}
